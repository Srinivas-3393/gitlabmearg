import random

def suggest_reviewers(title, description, changed_files):
    # Mock logic — replace with LLM/ML later
    reviewers = ["alice", "bob", "carol", "dave"]
    return random.sample(reviewers, 2)
